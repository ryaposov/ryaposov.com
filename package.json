{
  "name": "ryaposov",
  "version": "1.0.0",
  "description": "Blog and Portfolio",
  "author": "Pavel Ryaposov",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint",
    "deploy": "npm run build && gcloud app deploy app.yaml --project ryaposov"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^1.4.0",
    "babel-eslint": "^8.2.1",
    "eslint": "^5.16.0",
    "eslint-loader": "^2.0.0",
    "eslint-plugin-vue": "^7.0.0-alpha.0",
    "nodemon": "^1.11.0",
    "postcss-nested": "^4.2.1",
    "postcss-nested-ancestors": "^2.0.0"
  }
}
